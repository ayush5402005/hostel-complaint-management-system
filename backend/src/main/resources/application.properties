
# MySQL Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/hostel_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# IMPORTANT: Change 'root' to your actual MySQL password
spring.datasource.password=Ayush@2005

# Hibernate Configuration (Auto-generates tables)
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Set the port for our backend server so it doesn't clash with the frontend later
server.port=8080